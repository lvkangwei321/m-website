   <div class = 'list'>
    {{each list}}
             <div data-id = {{$value.goodsId}}>
                <img src="{{$value.goodsLogo}}"alt="">
                <h2>{{$value.goodsName}}</h2>
                <h3>{{$value.goodsIntro}}</h3>
                <h4>￥{{$value.goodsPrice}}</h4>
                <h5> 立即购买</h5>
        </div>
            {{/each}}
            </div>